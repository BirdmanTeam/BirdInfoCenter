<section class="content-wrapper cf">
  <article class="articles-wrapper">
        <%= render 'news/articles', :collection => @sport  %>
        <%= will_paginate @sport %>
  </article>
  <%= render 'news/sidebar', collection: @sport  %>
  <div class="weather">
    <%=raw @weather_api %>
  </div>
</section>
