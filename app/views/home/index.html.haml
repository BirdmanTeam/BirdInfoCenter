%section.content-wrapper.cf
  %article.articles-wrapper
    = render '/news/articles', :collection => @news
    = will_paginate @news
  = render '/news/sidebar', collection: @popular
  .weather
    = raw @weather_api