<section class="content-wrapper cf">
  <article class="articles-wrapper">
        <%= render 'news/articles', :collection => @music  %>
        <%= will_paginate @music %>
  </article>
  <%= render 'news/sidebar', collection: @music  %>
  <div class="weather">
    <%=raw @weather_api %>
  </div>

</section>