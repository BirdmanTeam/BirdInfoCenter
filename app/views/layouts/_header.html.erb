<% if user_signed_in? %>
    <div class="admin-panel">
      <h3>Hello, </h3>
      <% if current_user.admin? %>
          <p>admin</p>
          <h3>.</h3>
          <%= button_to 'Update news', {:controller => 'news', :action => 'parse_news'}, :method => 'get' %>
      <% else %>
          <p>user</p>
          <h3>.</h3>
      <% end %>

    </div>
<% end %>
<header>
  <section class="nav-wrapper cf">
    <%= link_to home_path do %>
        <div class="logo"></div>
    <% end %>
    <nav class="cf">
      <ul>
        <li><%= link_to 'Home', home_path %></li>
        <li><%= link_to 'Sport', sport_path %></li>
        <li><%= link_to 'Music', music_path %></li>
        <li><%= link_to 'Politic', politic_path %></li>
        <li><%= link_to 'Economics', economics_path %></li>
        <% if user_signed_in? %>
            <li><%= link_to 'Sign out', destroy_user_session_path, :method => 'delete' %></li>
        <% else %>
            <li><%= link_to 'Sign in', new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </section>
</header>