<section class="content-wrapper cf">
  <article class="articles-wrapper">
        <%= render 'news/articles', :collection => @economics  %>
        <%= will_paginate @economics %>
  </article>
  <%= render 'news/sidebar', collection: @popular  %>
  <div class="weather">
    <%=raw @weather_api %>
  </div>
</section>