<section class="content-wrapper cf">
  <article class="articles-wrapper">
        <%= render 'news/articles', :collection => @politic %>
        <%= will_paginate @politic %>
  </article>
  <%= render 'news/sidebar', collection: @politic  %>
  <div class="weather">
    <%=raw @weather_api %>
  </div>
</section>